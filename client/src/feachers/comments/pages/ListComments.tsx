//import { Row, Spin, Alert } from "antd";
//import { useGetCommentsTaskByIdQuery } from "../api/comments.api";
//import { useParams } from "react-router-dom";

export const ListComments = () => {
    /*const { taskId } = useParams()
    const { data, isLoading, error } = useGetComentsTaskByIdQuery(taskId!);

    console.log("Тип данных:", typeof data, "Это массив?", Array.isArray(data), "Значение:", data);//видалити цей рядок!!!
    if (isLoading) {
        return <Spin size="large" style={{ marginTop: 100 }} />;
    }

    if (error) {
        return <Alert type="error" message="Failed to load Comments! Create comments" />;
    } */

    

    return(
        <>
            
            <hr />  
                you on comments List
            <hr />

        </>
    )
}